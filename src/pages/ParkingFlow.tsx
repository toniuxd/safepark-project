import PageWrapper from "@/components/safepark/PageWrapper";

const ParkingFlow = () => (
  <PageWrapper>
    <p className="text-sp-text-secondary">Parking Flow — placeholder</p>
  </PageWrapper>
);

export default ParkingFlow;
